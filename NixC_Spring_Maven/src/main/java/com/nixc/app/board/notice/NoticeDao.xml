<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
  <mapper namespace="com.nixc.app.board.notice.NoticeDao">
  
  	<select id="detail" parameterType="NoticeVO" resultType="NoticeVO">
  		SELECT *
  		FROM notice
  		WHERE board_no = ${boardNo}
  	</select>
  
	<!-- ID는 DAO의 메서드명과 동일하게 작성 -->
	<insert id="insert" parameterType="NoticeVO">
		INSERT INTO notice(board_no, board_title, board_content, board_writer, board_date, board_hit)
		VALUES
		(NULL, #{boardTitle}, #{boardContent}, #{boardWriter}, now(), 0)
	</insert>
	<update id="update" parameterType="NoticeVO">
		UPDATE notice
		SET board_title = #{boardTitle}, board_content = #{boardContent}, board_writer = #{boardWriter}
		<where>
			board_no = #{boardNo};
		</where>
	</update>
	<delete id="delete" parameterType="NoticeVO">
		DELETE FROM notice
		WHERE board_no = #{boardNo}
	</delete>
	
	<select id="list" resultType="NoticeVO">
		SELECT *
		FROM notice
		ORDER BY board_no DESC
	</select>
	
	<insert id="add" parameterType="NoticeVO">
		INSERT INTO notice (board_title, board_content, board_writer, board_date, board_hit)
		VALUES (#{boardTitle}, #{boardContent}, #{boardWriter}, now(), 0);
	</insert>
	
</mapper>

