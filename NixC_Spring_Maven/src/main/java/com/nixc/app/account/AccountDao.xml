<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
 <mapper namespace="com.nixc.app.account.AccountDao">
	
<!-- 	<insert id="add" parameterType="java.util.Map"> -->
<!-- 		INSERT INTO account (account_no, member_id, product_no, account_date, account_balance) -->
<!-- 		VALUES -->
<!-- 		<foreach collection="list" item="productNo" separator=","> -->
<!-- 			(#{accountNo}, #{memberId}, #{productNo}, now(), 500) -->
<!-- 		</foreach>  -->
<!-- 	</insert> -->
	
	<!-- 선생님 풀이 -->
	<insert id="add" parameterType="java.util.List">
		INSERT INTO account (account_no, member_id, product_no, account_date, account_balance)
		VALUES
		<foreach collection="list" item="vo" separator=",">
			(#{vo.accountNo}, #{vo.memberId}, #{vo.productNo}, now(), 500)
		</foreach> 
	</insert>

	
</mapper>

